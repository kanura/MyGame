using UnityEngine;
using System.Collections;

public class Cloud01 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (this.gameObject, 5.0f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
